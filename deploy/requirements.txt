# NAS 部署用執行期相依(版本對齊開發機,確保 model.pkl 能原樣載入)
# —— 這幾個版本一定要對上,不然 xgboost/lightgbm/sklearn 反序列化 model.pkl 會失敗。
numpy==2.4.4
scipy==1.17.1
scikit-learn==1.8.0
xgboost==3.3.0
lightgbm==4.6.0
# pandas 開發機沒鎖版本;用相容 numpy 2.x 的近期版即可(不影響 model.pkl 載入)。
pandas>=2.2,<3

# —— 永豐 Shioaji(券商:庫存同步/手動下單/盤中即時報價)——
# 已啟用:①券商功能(庫存/帳戶總值/下單)在 NAS 上要用 ②model_env 環境閘門把 shioaji
# 列為必要套件,少了它 model.pkl 會被判「不可載入」→ 妖股判斷全停。cp314 x86_64 有 wheel。
# 登入永豐+CA 憑證仍是你自己在 NAS 完成,我不會、也不該代為登入券商。
shioaji==1.3.3
